package com.projet.chat_service.entity;

public enum ConversationStatus {
    ACTIVE,          // garde pour compatibilité
    EN_COURS,        // ← nouveau (cahier des charges)
    RESOLU,          // ← nouveau (cahier des charges)
    INCIDENT_CREE,   // ← nouveau (cahier des charges)
    FERMEE           // garde pour compatibilité
}