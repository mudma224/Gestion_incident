spring:
  cloud:
    gateway:
      mvc:
        globalcors:
          cors-configurations:
            '[/**]':
              allowed-origins: ${GATEWAY_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001}
              allowed-methods: "*"
              allowed-headers: "*"
              allow-credentials: true
